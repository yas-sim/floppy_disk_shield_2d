# kfx2bs.py

```sh
python kfx2bs.py --help
```
```
** FryoFlux RAW stream data to FD-Shield bitstream file converter (https://www.kryoflux.com/)
usage: kfx2bs.py [-h] -i INPUT [--clk_spd CLK_SPD]

optional arguments:
  -h, --help            show this help message and exit
  -i INPUT, --input INPUT
                        Directory name which contains input KryoFlux RAW
                        bitstream files
  --clk_spd CLK_SPD     clock speed for down-sampling (default=4MHz=4000000)
  ```
